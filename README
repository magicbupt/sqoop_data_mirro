项目名称：
    SQOOP_SEARCH_V3_VIEW
 
环境依赖: 
    Hadoop,sqoop,python
 
需求：
    同步search_v3_view,search_info视图数据,并把数据库镜像以json格式保存

数据存储说明：
    a)数据镜像存放地址:
         hdfs:/personal/search/share/表名/$current_day
    b)存放形式：
        按天存放；时间即为当天作业运行时间的前一天;
    c)文件格式：
        json格式，一条数据一行，一行以\n结束
    d)配置文件
        conf/conf.ini
